# Secrets — never commit a real .env (use .env.example as the template)
.env

# Hardhat build cache and compiled artifacts (regenerated by `npx hardhat compile`)
cache/
artifacts/

# Dependencies (regenerated by `npm install`)
node_modules/
